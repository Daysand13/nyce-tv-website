# Where the frontend should send API requests. Point this at your deployed backend URL
# (e.g. https://api.yourdomain.com/api) once it's live. Vite only reads variables that
# start with VITE_.
VITE_API_URL=http://localhost:4000/api
